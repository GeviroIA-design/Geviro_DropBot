# ============ Coeur scoring (existant) ============
BOT_SEED=42
N_PRODUCTS=30
FORECAST_HORIZON=14
OUTPUT_DIR=outputs
SAMPLE_CSV=data/sample_products.csv
LOG_LEVEL=INFO
USE_SAMPLE=false

# ============ Service 24/7 ============
# Persistance d'état (SQLite, stdlib).
STATE_DB_PATH=data/state.db
# Fichier de log (utilisé par /tail_logs).
LOG_FILE=logs/service.log
# Nombre de workers d'exécution.
WORKER_COUNT=2
# Cadence de la boucle superviseur (secondes).
TICK_INTERVAL=1.0
# Intervalles des jobs récurrents (secondes).
SCAN_INTERVAL=1800
EXPORT_INTERVAL=3600
# Heartbeat / watchdog.
HEARTBEAT_INTERVAL=15
HEARTBEAT_MAX_AGE=90
WATCHDOG_INTERVAL=30
JOB_TIMEOUT=300

# ============ Retry / Circuit breaker ============
MAX_RETRIES=4
RETRY_BASE_DELAY=2.0
RETRY_FACTOR=2.0
RETRY_MAX_DELAY=300
CB_FAILURE_THRESHOLD=5
CB_RECOVERY_TIMEOUT=60

# ============ Telegram admin ============
# Token du bot (BotFather). VIDE => console Telegram désactivée (no-op sûr).
TELEGRAM_BOT_TOKEN=
# IDs Telegram admin autorisés, séparés par des virgules. Ex: 123456789,987654321
TELEGRAM_ADMIN_IDS=
# Timeout du long-polling getUpdates (secondes).
TELEGRAM_POLL_TIMEOUT=30
# Rate limit : nb max de commandes par admin et par minute.
TELEGRAM_RATE_LIMIT=20
# Nb de lignes par défaut pour /tail_logs.
TAIL_LOG_LINES=20

# ============ Revente (resale) + garde-fous ============
# Canal de vente : "simulated" (défaut) ; "ebay"/"shopify" quand branchés.
SALES_CHANNEL=simulated
# Frais de la marketplace (ex eBay ~12%), pour calculer la marge nette.
EBAY_FEE_PCT=0.12
# Marge nette minimale par vente (EUR) pour proposer / mettre en vente.
MIN_MARGIN_EUR=3
# Quota de mises en vente par 24h.
MAX_LISTINGS_PER_DAY=20
